import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

// Mutation-types

// State
const state = {};

// Actions
const actions = {};

// Mutations
const mutations = {};

export default new Vuex.Store({
  state,
  mutations,
  actions,
});
