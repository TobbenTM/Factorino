apiVersion: v1
kind: Service
metadata:
  name: {{ template "factorino-webapp.fullname" . }}
  labels:
    app: {{ template "factorino-webapp.name" . }}
    chart: {{ template "factorino-webapp.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "factorino-webapp.name" . }}
    release: {{ .Release.Name }}
