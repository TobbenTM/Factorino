apiVersion: v1
kind: Service
metadata:
  name: {{ template "factorino-readmodel.fullname" . }}
  labels:
    app: {{ template "factorino-readmodel.name" . }}
    chart: {{ template "factorino-readmodel.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "factorino-readmodel.name" . }}
    release: {{ .Release.Name }}
