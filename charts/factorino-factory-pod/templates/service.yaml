apiVersion: v1
kind: Service
metadata:
  name: {{ template "factorino-factory-pod.fullname" . }}
  labels:
    app: {{ template "factorino-factory-pod.name" . }}
    chart: {{ template "factorino-factory-pod.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "factorino-factory-pod.name" . }}
    release: {{ .Release.Name }}
